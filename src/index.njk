<!DOCTYPE html>
<html lang="en">

<head>
  <meta charset="utf-8">
  <meta name="viewport" content="width=device-width">
  <title>Pets of Netlify</title>
  <link rel="stylesheet" href="https://fonts.googleapis.com/css?family=Roboto:400,500,700">
  {# Include the CSS and minify it with a filter #}
  {% set css %}
    {% include "style.css" %}
  {% endset %}
  <style>
    {{ css | cssmin | safe }}
  </style>
</head>

<body>
  <header>
    <h1>Pets of Netlify</h1>
    <p>Say hello to our little friends</p>
  </header>
  <main class="pets">
  {# Output some HTML for each pet found in _data/pets.json #}
  {% for pet in pets %}
    <a class="pet" href="#{{ pet.name | slug }}" id="{{ pet.name | slug }}">
      <h2>{{ pet.name }}</h2>
      <img src="/cloudinaried/200/{{ pet.photo }}" alt="{{ pet.name }}" loading="lazy">
    </a>
  {% endfor %}
  </main>
  <footer>
    Made with <img class="netliheart" title="love" src="/images/netliheart.svg" alt="love" width="1em"> by the <a href="https://netlify.com?utm_source=web&utm_medium=petsofnetlify-pnh&utm_campaign=devex">Netlify</a> team. Explore <a href="https://github.com/netlify/petsofnetlify">the code</a> in GitHub. Follow <a href="https://twitter.com/netlify">@Netlify</a> on Twitter for regular updates</a>.
  </footer>
</body>
</html>
